import React from 'react'

type FormInputProps = {
    type: string,
    formDataAttr: string,
    title: string,
    placeholder: string,    
}

const FormInput = ({ type, formDataAttr, title, placeholder } : FormInputProps) => {
  
  if(type == 'text'){
    
  }

  return (
    <div>

    </div>
  )
}

export default FormInput